services:
  cliparr:
    build: .
    ports:
      - "3000:3000"
    environment:
      APP_KEY: "${APP_KEY:?APP_KEY is required}"
      PORT: "3000"
      CLIPARR_DATA_DIR: "/data"
    volumes:
      - cliparr-data:/data
    restart: unless-stopped

volumes:
  cliparr-data:
