services:
  cliparr:
    build: .
    ports:
      - "3000:3000"
    environment:
      APP_URL: "http://localhost:3000"
      PORT: "3000"
      CLIPARR_DATA_DIR: "/data"
    volumes:
      - cliparr-data:/data
    restart: unless-stopped

volumes:
  cliparr-data:
