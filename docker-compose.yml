services:
  cliparr:
    build: .
    ports:
      - "7171:7171"
    environment:
      APP_KEY: "${APP_KEY:?APP_KEY is required}"
      PORT: "7171"
      CLIPARR_DATA_DIR: "/data"
    volumes:
      - cliparr-data:/data
    restart: unless-stopped

volumes:
  cliparr-data:
