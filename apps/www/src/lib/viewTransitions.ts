// Chrome can abort the animation when its mobile address bar resizes the viewport.
export const viewportTransitionAbort =
  /Transition was aborted because of invalid state\. Viewport size changed/;
